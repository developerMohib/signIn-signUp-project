
import { Link } from 'react-router-dom';

const ErrorPage = () => {
    return (
        <div>
            <Link to='/' className='border p-4 rounded-lg' > Go to Home </Link>
        </div>
    );
};

export default ErrorPage;