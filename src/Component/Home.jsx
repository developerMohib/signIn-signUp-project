
const Home = () => {
    // const authInfo = useContext( AuthContext );
    return (
        <div className="h-[80vh] flex justify-center items-center" >
            <h1>This is Home  </h1>
        </div>
    );
};

export default Home;